import React, { Component } from "react";

const styles = {
  superNav:{
    display:'flex',
    justifyContent: "space-between"
  }
}

export default class SuperNav extends Component{
  render(){
    return(
    <div style={styles.superNav}>
      
    </div>
    )
  }
}
