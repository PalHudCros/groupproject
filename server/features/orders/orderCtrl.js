import Order from './Order'
import User from '../users/User'
import mongoose from 'mongoose'

module.exports = {
  makeOrder(req, res, next){
    next()
  }
  , addOrderStatusToUser(req, res, next){
    next()
  }
  , deleteCartAndSession(req, res, next){
    next()
  }
}
